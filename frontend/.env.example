# Copy this file to .env.local and fill in your values.
# .env.local is git-ignored — NEVER commit real secrets.

# ── Supabase (public — safe to expose in browser) ──
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ── Supabase (server-side only — bypasses RLS) ──
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ── Google Gemini API (server-side only) ──
# Used by: analyze, extract-resume, interview-questions, summary-generator, cover-letter
# Get your key: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# ── ML Server URL ──
# Local development: http://127.0.0.1:8100 (run ml-server locally)
# Production: your Hugging Face Space URL
ML_SERVER_URL=https://your-hf-space.hf.space

# ── Resend (transactional email — server-side only) ──
# Get your key: https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here
NEWSLETTER_FROM=Your App <onboarding@resend.dev>
